export enum ExamType {
  IELTS  = 'IELTS',
  PTE    = 'PTE',
  TOEFL  = 'TOEFL',
  GRE    = 'GRE',
}

export enum PlanType {
  BASIC        = 'basic',
  PROFESSIONAL = 'professional',
  ELITE        = 'elite',
}

export enum EnrollmentStatus {
  ACTIVE    = 'active',
  EXPIRED   = 'expired',
  CANCELLED = 'cancelled',
  PENDING   = 'pending',
}
